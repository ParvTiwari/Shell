# Shell
This project is a lightweight implementation of a Unix-like command-line shell written in C. It supports core shell functionalities such as command execution, piping, input/output redirection, background processes, and command history.
